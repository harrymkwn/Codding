#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define F(a, b, i) for (int i = a; i < b; i++)
#define f(a, b, i) for (int i = a; i >= b; i--)
#define m 1000000007

int main(){
    int n,x;
    cin>>n;
    vector<vector<int>> hash(5000);
    int hashed=0;
    F(0,n,i){
        cin>>x;
        for(0,x,j){
            cin>>y;
            hashed = 
        }
    }
  return 0;
}